<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.projettic.dao.AccountDao">

    <!-- 此处select标签的id值对应Mapper类中方法名 -->
    <select id="testQuery" parameterType="java.lang.String" resultType="java.util.LinkedHashMap">
        <!-- 此处写sql语句,#{Mapper类传入的参数} -->
        ${sqlquery}
    </select>

    <select id="getHisResult" parameterType="java.lang.String" resultType="java.util.LinkedHashMap">
        <!-- 此处写sql语句,#{Mapper类传入的参数} -->
        ${sqlquery}
    </select>


</mapper>
